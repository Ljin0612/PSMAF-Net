# UNIV integration notes

This directory is reserved for the original UNIV upstream code path. The current repository keeps UNIV isolated under `third_party/UNIV/` so PSMAF-Net development can remain clean and independent from legacy experiments.

If the official UNIV source is replaced or refreshed, preserve this boundary and avoid adding datasets, checkpoints, logs, or downstream experiment artifacts.
